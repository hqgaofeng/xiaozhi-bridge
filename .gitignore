# --- Python (bridge) ---
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/

# Config with secrets
config/config.yaml
config/*.local.yaml
config/secrets.yaml

# --- Node (web) ---
node_modules/
web/dist/
web/.vite/
web/.env
web/.env.local
web/.env.production
# tsc -b composite emits metadata + .d.ts/.js for vite.config.ts
# (we don't ship the .d.ts, the source-of-truth is vite.config.ts)
web/*.tsbuildinfo
web/vite.config.d.ts
web/vite.config.js

# Logs
*.log
logs/

# Editor
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# OS
.Trash-*

# Generated
*.bak
*.tmp

# Local secrets / runtime configs (contain tokens, never commit)
config/openclaw.json

# Local .env files (root - web/.env* already covered above)
.env
.env.local
.env.production
config/config.yaml.bak.20260604-191759
